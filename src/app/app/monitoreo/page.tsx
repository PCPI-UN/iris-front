import { MonitoringDashboard } from '../_components/monitoring-dashboard';

export const metadata = {
  title: 'Monitoreo',
  description: 'Monitoreo del administrador',
};

const MonitoringPage = () => {
  return <MonitoringDashboard />;
};

export default MonitoringPage;